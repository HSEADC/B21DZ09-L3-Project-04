 <%#= form_with(model: [task, answer]) do |form| %>
 <%= form_for  [@task, @answer] do |f| %>

  <% if answer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(answer.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



    <%#= render 'task_cards/card_one_button_big', task: task%>

  <div class="field">
    <%= form.label :answer_image %><br />
    <%= form.file_field :answer_image %>
  </div>

	<div class="actions">
		<%= form.submit %>
      <%= @task %>
	</div>
<% end %>
