<%
  alert = current_user.alert
  alert_maximum = 3
  alert_total = alert.count
  i = 1
%>

<section id="alert">
  <div class="background"></div>
  <div class="S_Alert"> 
    <div class="attention">
      <div class="attention-icon"></div>
      <div class="circles">
        <% alert_maximum.times do %>
          <div class="circle <%= i <= alert_total ? 'active' : 'inactive' %>"></div>
          <% i += 1 %>
        <% end %>
      </div>
    </div>
    
    <div class="text-block">
    <% if alert_total < alert_maximum%>
      <p>У тебя уже <%= alert.count%> замечания, после <%= alert_maximum%> твой аккаунт заблокируется</p>
      <%= link_to 'я все понял', alert_passed_path, class:"M_Button primary red s" %>
    <% end %>

    <% if alert_total = alert_maximum%>
      <p>У тебя <%= alert.count%> замечания! Как мы предупреждали, твой аккаунт теперь заблокирован.</p>
      <%= link_to 'аккаунт будет заблокирован', alert_blocked_path, class:"M_Button primary red s" %>
    <% end %>
    </div>
  </div>
</section>